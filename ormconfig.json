{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "marianeviaba",
  "password": "",
  "database": "BookApp", 
  "entities": [
     "src/models/*.ts"
  ],
  "migrations": [
     "src/database/migrations/*.ts"
  ],

  "cli":{
     "migrationsDir": "src/database/migrations"
  }
}
